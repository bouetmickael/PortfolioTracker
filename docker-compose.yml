services:
  portfolio-tracker:
    build: .
    container_name: portfolio-tracker
    restart: unless-stopped
    env_file: .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - ./data:/data
